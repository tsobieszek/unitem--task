# Add exception handling
# Add remaining docstrings
# Test main()
# Add more test samples OR (better)
# Add hypothesis properties-based tests
# Add logging
# Add a config file and put there all the constants from main()
# Add comments about GIL (better to use processes than threads) ?
